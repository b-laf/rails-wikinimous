<div>
  <h2>Wikiminous</h2>
  <%= link_to "New article", new_article_path %>
</div>

<div class="container">
  <% @articles.each do |article| %>
    <%= link_to article_path(article) do %>
      <%= article.title %>
    <% end %>
    <p><%= article.content %></p>
  <% end %>
</div>
